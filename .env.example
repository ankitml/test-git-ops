# Configuration for Community Sync Testing Framework
# Copy this file to .env and update with your actual paths

# Path to your local enterprise repository (with custom patches)
ENTERPRISE_REPO=/path/to/your/enterprise-repo

# URL to the upstream community repository
COMMUNITY_REPO_URL=https://github.com/your-org/community-repo.git

# Optional: Custom test directory (defaults to /tmp/test-community-sync)
# TEST_DIR=/custom/path/test-repos

# Optional: Virtual environment directory (defaults to .venv)
# VENV_DIR=.venv